[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 120
target-version = "py311"
extend-exclude = [".deps", "pip-*"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
